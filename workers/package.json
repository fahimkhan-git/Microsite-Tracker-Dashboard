{
  "name": "microsite-tracker-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for microsite tracking (hybrid approach)",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "tail:prod": "wrangler tail --env production"
  },
  "keywords": ["cloudflare", "workers", "tracking"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.50.0"
  }
}

